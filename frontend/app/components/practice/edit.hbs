{{yield}}
<Jumbo>
  <h2> {{@practice.name}} </h2>
</Jumbo>
<form {{on "submit" (fn this.editPractice @practice)}}>
  <dl>
    <dt>Naam : </dt>
    <dd>
      <Input class="inputfield" @value={{@practice.name}} @placeholder="naam" required />
    </dd>
    <dt>Straat :</dt>
    <dd>
      <Input class="inputfield" @value={{@practice.street}} @placeholder="straat" required />
    </dd>
    <dt>Huisnummer :</dt>
    <dd>
      <Input class="inputfield" @value={{@practice.housenumber}} @placeholder="huisnr" required />
    </dd>
    <dt>Postcode :</dt>
    <dd>
      <Input class="inputfield" @value={{@practice.postalcode}} @placeholder="postcode" required />
    </dd>
    <dt>Gemeente :</dt>
    <dd>
      <Input class="inputfield" @value={{@practice.city}} @placeholder="gemeente" required />
    </dd>
    <dt>Type Praktijk :</dt>
    <dd>
      <Input class="inputfield" @value={{@practice.type}} @placeholder="type" required />
    </dd>
  </dl>
  <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="submit">Save</button>
</form>